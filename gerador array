private static int[] geradorArray(int n){
        Random gerador = new Random();
        int []v = new int[n];
        for(int i = 0; i < n; i++){
            v[i] = gerador.nextInt(n);
        }
        return v;
    }
